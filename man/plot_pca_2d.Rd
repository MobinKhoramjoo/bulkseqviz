% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pca_2d.R
\name{plot_pca_2d}
\alias{plot_pca_2d}
\title{2D PCA Plot}
\usage{
plot_pca_2d(
  bs_obj,
  color_by,
  ellipse_by = NULL,
  subset_samples = NULL,
  min_gene_counts = 10,
  ntop = 500,
  pcs = c(1, 2),
  ellipse_level = 0.99,
  group_colors = NULL
)
}
\arguments{
\item{bs_obj}{A \code{bulkseq} object created by \code{create_bulkseqvis_object}.}

\item{color_by}{Character string. Metadata column for point color.}

\item{ellipse_by}{Character string. Optional. Metadata column for grouping ellipses.}

\item{subset_samples}{Character vector. Optional. Subset of sample IDs to include.}

\item{min_gene_counts}{Integer. Keep genes with >= this many total reads. Default 10.}

\item{ntop}{Integer. Number of most variable genes to calculate PCA on. Default 500.}

\item{pcs}{Numeric vector of length 2. Which PCs to plot (e.g., c(1, 2)). Default c(1, 2).}

\item{ellipse_level}{Numeric. Confidence level for ellipses. Default 0.99.}

\item{group_colors}{Character vector. Optional. Custom colors. Defaults to ggsci::nrc_npg.}
}
\value{
A ggplot object.
}
\description{
Generates a 2D PCA plot using VST-transformed counts. Supports custom PC selection
and ellipses around groups.
}
\examples{
\dontrun{
  plot_pca_2d(my_obj, color_by = "condition")
  plot_pca_2d(my_obj, color_by = "condition", pcs = c(3, 4))
}
}
