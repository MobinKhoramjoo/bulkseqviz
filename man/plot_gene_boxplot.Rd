% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene_boxplot.R
\name{plot_gene_boxplot}
\alias{plot_gene_boxplot}
\title{Gene Expression Boxplot}
\usage{
plot_gene_boxplot(
  bs_obj,
  genes,
  x_var,
  region_var,
  x_order = NULL,
  region_order = NULL,
  x_colors = NULL,
  jitter_points = TRUE,
  normalize_to_baseline = FALSE,
  log2_transform = FALSE,
  show_ns = TRUE,
  biomart_dataset = "hsapiens_gene_ensembl"
)
}
\arguments{
\item{bs_obj}{A \code{bulkseq} object.}

\item{genes}{Character vector. Gene IDs or Symbols to plot (must match rownames of counts).}

\item{x_var}{Character string. Metadata column for x-axis.}

\item{region_var}{Character string. Metadata column for faceting.}

\item{x_order}{Character vector. Optional. Order of levels for x-axis.}

\item{region_order}{Character vector. Optional. Order of levels for facet.}

\item{x_colors}{Named character vector. Optional. Colors for x-axis groups.}

\item{jitter_points}{Logical. Add jitter points? Default TRUE.}

\item{normalize_to_baseline}{Logical. Normalize expression to the median of the first x-level? Default FALSE.}

\item{log2_transform}{Logical. Log2 transform counts? Default FALSE.}

\item{show_ns}{Logical. Show non-significant brackets? Default TRUE.}

\item{biomart_dataset}{Character string. BioMart dataset for symbol mapping (e.g., "hsapiens_gene_ensembl").
Default "hsapiens_gene_ensembl". Set to NULL to skip fetching.}
}
\value{
A ggplot object.
}
\description{
Generates boxplots for specific genes across conditions with statistical comparisons.
Automatically fetches gene symbols via BioMart if needed.
}
\examples{
\dontrun{
  plot_gene_boxplot(bs_obj, genes = c("ENSG00000139618"),
                    x_var = "condition", region_var = "batch")
}
}
